# 钢琴相对音感训练网站 - 项目提示词

## 使用说明

把下面「完整提示词」整段复制给任何 AI 编程工具（Cursor、Claude、Codex、通义灵码等），它就可以直接帮你生成一个可部署的前端项目。如果想快速试一次，也可以用文末的「最短版本」。

---

## 完整提示词

请帮我从零搭建一个纯前端的钢琴相对音感训练网站，名为「钢琴耳训练」或类似的中文名称。它不需要后端、不需要数据库、不需要登录，目标是让我用电脑或手机练习「听几个音、再弹出来」的相对音感。

### 技术栈

- Vite + React + TypeScript（如果工具不擅长 React，也可以用原生 HTML/CSS/JS，但必须是单页应用）。
- 所有钢琴声音用 Web Audio API 实时合成，不要引入任何外部音频文件、图片素材或字体 CDN。
- 不需要任何后端；构建产物必须是纯静态文件，能一键部署到 Vercel、Netlify 或 Cloudflare Pages。
- 状态管理尽量简单（React 内置 state 即可，最多加一个轻量库）。
- 使用 CSS 变量和响应式布局，桌面端与手机端都要完整可用。
- 代码注释保持中文，界面文案全部使用简体中文。

### 核心功能

1. 钢琴键盘
   - 用 HTML/CSS 绘制一个真实的钢琴键盘，包含白键和黑键的正确视觉关系（黑键位于两个白键之间、长度更短）。
   - 键盘本身展示完整 88 键（A0-C8），可通过左右按钮、滚轮、拖动滚动条或触摸滑动滚动到任意琴键；出题音域单独用设置控制（默认 C4-C5，可在 C1-C8 间自由调整）。
   - 手机端支持双指捏合缩放琴键大小，缩放以双指中心为锚点，并提供一键恢复默认大小的重置按钮。
   - 桌面端用鼠标点击；手机端要能通过手指按下一个键并立即出声，支持多指同时按。
   - 按下时键盘有视觉反馈，松手时恢复。

2. 听音训练模式
   - 设置项：
     - 音域范围（可选起始音和结束音）。
     - 设置面板实时显示当前出题范围（例如 C3 - C5），题目必须严格落在该范围内。
     - 音符来源：只出白键 / 白键黑键都出。
     - 序列长度：1 个音、2 个音、3 个音、4 个音、5 个音（每次生成 1-5 个音符的随机序列）。
     - 自动播放次数：1 遍、2 遍、3 遍。
     - 播放示例时是否高亮琴键（可开关）。
     - 是否在弹完后展示正确答案。
   - 开始练习后，网站自动按顺序播放目标序列 1-3 遍（播放间隔约 0.6 秒，音与音之间清楚分开）。
   - 点击「下一题」后即可进入「跟弹」阶段，我在钢琴键盘上依次弹出我听到的音，播放会继续，可以边听边弹。

3. 实时判断与反馈
   - 我每按一个键，系统立刻判断这个音是否与目标序列当前位置相同。
   - 正确：按键短暂变绿，并记录为正确。
   - 错误：按键短暂变红，可以提示「第 N 个音不对，再听一遍」并自动重播当前音或整段序列。
   - 全部弹完且正确时，显示本次练习结果（用时、错误次数、答对率），并提供「下一题」和「重听」按钮。
   - 跟弹判断固定为绝对音高：必须弹出与目标完全相同的音。

4. 辅助功能
   - 「重听当前序列」按钮，随时可以再播放一遍。
   - 「显示答案」按钮，展示目标序列对应的简谱或音名（如 C D E）。
   - 练习统计：本次练习答对题数、总题数、正确率，用 localStorage 保存最近记录。
   - 完成庆祝：全部弹对后播放上扬音符音效和音符飘动特效，可在设置中关闭，默认开启。
   - 完成评分：全部弹对为 5 星，其余按弹错次数降星，与用时无关。
   - 可选：支持连接 USB 或蓝牙 MIDI 键盘（Web MIDI API），检测到设备后自动允许用实体键盘演奏。如果环境不支持 MIDI，不影响其他功能。

### 视觉风格（必做）

- 整体走「浅色系 + 毛玻璃 + 流光」的清爽通透质感，不要用黑色或深色背景，也不要做成普通白底网页。
- 背景：浅色柔和渐变作为底，例如淡蓝、浅紫、奶白之间的过渡，叠加 2-3 条缓慢流动的柔彩光带（例如淡青、浅粉、淡金），光带要柔和、低饱和、有通透感，不能抢过钢琴键盘和文字。
- 毛玻璃：设置面板、结果卡片、进度提示、统计区域都使用半透明白色玻璃层，配合 `backdrop-filter: blur()`、细高光边框和柔和阴影，让界面有通透的层次感；面板内文字统一用深色，保证浅色背景上的可读性。
- 流光细节：
  - 背景光带持续缓慢流动，方向、速度可以不同。
  - 钢琴键盘下方或边缘有一条细流光带扫过，表示「正在播放 / 等待跟弹」等状态。
  - 按键正确/错误反馈时，按键表面短暂出现绿色或红色的流光扫过效果。
  - 顶部标题或 Logo 可以做渐变流光文字，但频率要低，避免刺眼。
- 钢琴键盘本身保持真实琴键配色：白键用白色或很浅的暖白，黑键保留深色，方便一眼区分，只是界面背景和面板整体走浅色系。
- 动画实现尽量只使用 `transform` 和 `opacity`，避免频繁触发重绘；手机端模糊半径不要过大、光带数量不要超过 3 条，保证低端手机流畅。
- 支持 `prefers-reduced-motion`：用户系统开启减弱动态效果时，停止流光和扫过动画，只保留静态的毛玻璃质感。
- 所有玻璃面板内部文字必须保持足够对比度，任何流光都不能盖住琴键标签、按钮文字或判断结果。

### 页面与交互流程

- 首屏直接是训练工具本身，不要做营销页或介绍页。
- 建议布局：
  - 顶部：网站名 + 当前练习状态（准备播放 / 播放中 / 请跟弹 / 完成）。
  - 中间：目标进度提示（如「第 2 / 4 个音」）和判断结果区域。
  - 下方：设置面板，可收起；设置修改后立即生效或点击「开始练习」后生效。
  - 底部：固定钢琴键盘（手机端尤其要固定，方便随时弹）。
- 手机上设置面板与键盘不要互相遮挡；键盘宽度可横向滑动，但当前音域内所有白键应尽量同时可见。
- 每个按钮必须有明确的文案或图标，重要操作至少保留文字。

### 技术实现要点

- 音色：用 6-12 个带非谐性偏移的泛音振荡器做加法合成，高次泛音衰减更快，叠加短促的琴槌噪声和动态压缩，做出更接近真实钢琴的音色；不要用刺耳的高频方波。
- 键盘数据：用 MIDI 音符编号（如 C4 = 60）作为唯一标识，显示音名时用 C4、Db4 这种记法。
- 随机出题：生成的序列要有随机性，但相邻音之间不要太难识别；避免连续生成完全相同的音。
- 实时判断：用当前目标索引比较，不要整段一次性比较；弹错后允许重试当前音，不要直接判整题失败。
- 响应式断点：建议以 768px 为界区分手机和桌面布局；手机端黑键触控区域要足够大（至少 20px 宽）。
- 纯静态部署：Vite 配置 `base: "./"`，保证构建后直接打开或部署到任意静态托管平台都能用。
- README 中写清楚三条部署路径：Vercel、Netlify、Cloudflare Pages，并附上各平台「一键导入 GitHub 仓库」的操作说明，以及直接拖拽 `dist` 文件夹部署的方式。

### 验收标准

- 在 Chrome 桌面端打开项目，能完成一轮完整练习：设置音域 → 开始 → 听 1-3 遍 → 弹出目标音 → 得到正确/错误反馈 → 完成统计。
- 手机浏览器（建议 Chrome 或 Safari）上钢琴键盘可正常触摸演奏，训练流程完整。
- 设置「只出白键」时，目标序列不会包含黑键；设置为「黑白键都有」时，会按概率出现黑键。
- 跟弹时，只有弹出与目标完全相同的音才判定为正确。
- 页面刷新后保留最近设置和练习统计。
- 手机端开启毛玻璃和流光动画后仍能流畅演奏，无明显掉帧或琴键触摸延迟。
- 项目执行 `npm run build` 成功，产物可直接部署。

### 交付内容

给出完整可运行的 React 项目源码、依赖清单、启动命令、构建命令，以及中文 README（包含功能说明和三种平台的一键部署步骤）。

---

## 最短版本

做一个纯前端钢琴相对音感训练网站：Vite + React + TypeScript，中文界面，兼容手机和 PC，视觉走「浅色系 + 毛玻璃 + 流光」风格：淡蓝、浅紫、奶白的柔和渐变背景配缓慢流动的柔彩光带，设置面板和结果卡片使用半透明白色毛玻璃与细高光边框，钢琴键盘边缘有状态流光，按键正确/错误时有流光反馈，面板文字用深色保证可读性，动画只用 transform/opacity 且支持 prefers-reduced-motion，保证手机流畅。键盘展示完整 88 键（A0-C8）并支持左右滚动，播放示例时是否高亮琴键可配置。核心功能是：设置出题音域、只出白键或黑白都出、序列长度 1-5 个音、自动播放 1-3 遍；我用网页钢琴键盘跟弹，系统逐音实时判断对错并给反馈，判断固定为绝对音高（必须弹出与目标完全相同的音）；音色用 Web Audio API 多泛音加法合成，不需要音频文件和后端；统计存在 localStorage。底部固定钢琴键盘，手机可触摸多指弹奏。最终构建为纯静态文件，能一键部署到 Vercel、Netlify 或 Cloudflare Pages，并写好中文 README 部署说明。
